# General
.DS_Store
*.log
*.lock
*.sublime-*

# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
*.venv/
venv/
env/
.env
*.pytest_cache/
.mypy_cache/
.ruff_cache/
*.hypothesis/

# Node / Web
node_modules/
.npm/
dist/
build/
.parcel-cache/
.cache/
*.env.local
*.env.*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Rust
target/
Cargo.lock
# Ignoring Cargo.lock is often debated. If you want to ensure reproducible builds across environments,
# you might want to commit it. For library development, it's usually ignored.
# For an application, you might want to commit it. I'll ignore it for now.
# Remove the line above if you want to commit Cargo.lock.

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
vendor/
# Similar to Cargo.lock, the 'vendor/' directory can be committed or ignored.
# Ignoring it means `go mod download` will be needed.
# I'll ignore it for now.

# IDE specific
.idea/
.vscode/ 